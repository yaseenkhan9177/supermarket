<!DOCTYPE html>
<html lang="en">

<head>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>

<body class="bg-gray-100 font-sans text-gray-800">

    <!-- Header with Logo and Home Icon -->
    <div class="bg-white shadow-md border-b border-gray-200 sticky top-0 z-50">
        <div class="container mx-auto px-6 py-4">
            <div class="flex items-center justify-between">
                <!-- Logo Section -->
                <div class="flex items-center gap-3">
                    <img src="{{ asset('images/logo.png') }}" alt="Logo" class="h-10 w-10 rounded-lg shadow-sm">
                    <div>
                        <h1 class="text-xl font-black text-gray-900 tracking tight">
                            OwnStore <span class="text-xs uppercase tracking-widest text-indigo-500 ml-1">PRO</span>
                        </h1>
                        <p class="text-xs text-gray-500">Employee Management</p>
                    </div>
                </div>

                <!-- Home Icon -->
                <a href="/admin" class="flex items-center gap-2 px-4 py-2 bg-indigo-600 hover:bg-indigo-700 text-white rounded-lg shadow transition group">
                    <i class="fas fa-home"></i>
                    <span class="hidden sm:inline">Dashboard</span>
                </a>
            </div>
        </div>
    </div>

    <div class="container mx-auto p-4 max-w-7xl h-screen flex gap-6">

        @if(session('success'))
        <script>
            Swal.fire({
                icon: 'success',
                title: 'Success!',
                text: '{{ session('
                success ') }}',
                timer: 3000,
                showConfirmButton: false,
                toast: true,
                position: 'top-end'
            });
        </script>
        @endif

        <!-- Left Sidebar: Team Directory -->
        <div class="w-1/3 bg-white rounded-xl shadow-sm border border-gray-200 flex flex-col">
            <div class="p-5 border-b">
                <div class="flex justify-between items-center mb-4">
                    <h2 class="text-xs font-bold text-gray-400 uppercase tracking-wider">Team Directory</h2>
                    <a href="{{ route('employees.web.index') }}" class="bg-indigo-600 text-white text-xs font-bold px-3 py-1.5 rounded hover:bg-indigo-700 transition">
                        + NEW
                    </a>
                </div>
                <div class="relative">
                    <i class="fas fa-search absolute left-3 top-3 text-gray-400 text-sm"></i>
                    <input type="text" id="searchInput" placeholder="Search by Name, Email, or Phone..." class="w-full pl-10 pr-4 py-2.5 bg-gray-50 border border-gray-200 rounded-lg text-sm focus:outline-none focus:border-indigo-500 transition">
                </div>
            </div>

            <div class="flex-1 overflow-y-auto p-3 space-y-3">
                @forelse($employees as $emp)
                <a href="{{ route('employees.edit', $emp->id) }}"
                    class="employee-card p-4 bg-white border {{ isset($employee) && $employee->id == $emp->id ? 'border-indigo-500 bg-indigo-50' : 'border-gray-100' }} rounded-xl hover:shadow-md hover:border-indigo-200 cursor-pointer transition group flex items-start gap-4"
                    data-name="{{ strtolower($emp->full_name) }}"
                    data-email="{{ strtolower($emp->email ?? '') }}"
                    data-phone="{{ $emp->phone ?? '' }}">
                    <div class="w-10 h-10 rounded-full {{ isset($employee) && $employee->id == $emp->id ? 'bg-indigo-600 text-white' : 'bg-indigo-100 text-indigo-600 group-hover:bg-indigo-600 group-hover:text-white' }} flex items-center justify-center font-bold text-sm transition">
                        {{ strtoupper(substr($emp->full_name, 0, 2)) }}
                    </div>

                    <div class="flex-1">
                        <div class="flex justify-between items-start">
                            <h3 class="font-bold text-gray-900 text-sm">{{ $emp->full_name }}</h3>
                            <span class="w-2 h-2 rounded-full {{ $emp->is_active ? 'bg-green-500' : 'bg-gray-300' }}"></span>
                        </div>
                        <p class="text-xs text-gray-500 mb-1">{{ $emp->designation }}</p>
                        <p class="text-[10px] text-gray-400 font-mono"><i class="fas fa-envelope mr-1"></i> {{ $emp->email ?? 'No email' }}</p>
                    </div>
                </a>
                @empty
                <div class="text-center text-gray-400 py-8">
                    <i class="fas fa-users text-4xl mb-2"></i>
                    <p>No employees yet</p>
                </div>
                @endforelse
            </div>
        </div>

        <!-- Right Panel: Create/Edit Form -->
        <div class="w-2/3">
            <div class="flex justify-between items-center mb-6">
                <div>
                    <h6 class="text-xs font-bold text-gray-400 uppercase tracking-widest">Human Resources</h6>
                    <h1 class="text-2xl font-bold text-gray-900">{{ isset($employee) ? 'Edit Employee' : 'Create New Employee' }}</h1>
                </div>
                <div class="flex gap-2">
                    @if(isset($employee))
                    <a href="{{ route('employees.web.index') }}" class="bg-gray-200 text-gray-700 px-6 py-2.5 rounded-lg shadow hover:bg-gray-300 transition font-medium text-sm">
                        Cancel
                    </a>
                    @endif
                    <button type="submit" form="empForm" class="bg-gray-900 text-white px-6 py-2.5 rounded-lg shadow hover:bg-gray-800 transition font-medium text-sm">
                        <i class="fas fa-save mr-2"></i> {{ isset($employee) ? 'Update' : 'Save' }} Profile
                    </button>
                </div>
            </div>

            <form id="empForm" action="{{ isset($employee) ? route('employees.update', $employee->id) : route('employees.store') }}" method="POST" class="space-y-6 overflow-y-auto h-[calc(100vh-140px)] pr-2">
                @csrf

                <!-- Identity & Contact Card -->
                <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
                    <div class="flex items-center gap-2 mb-6 text-indigo-600">
                        <i class="far fa-id-card text-lg"></i>
                        <h3 class="font-bold text-gray-900 text-lg">Identity & Contact</h3>
                    </div>

                    <div class="grid grid-cols-1 gap-6">
                        <div>
                            <label class="block text-xs font-bold text-gray-500 uppercase mb-2">Full Name*</label>
                            <input type="text" name="full_name" value="{{ isset($employee) ? $employee->full_name : '' }}" required placeholder="e.g. Muhammad Ali" class="w-full bg-gray-50 border border-gray-200 p-3 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none">
                        </div>

                        <div class="grid grid-cols-2 gap-6">
                            <div>
                                <label class="block text-xs font-bold text-gray-500 uppercase mb-2">Phone / Mobile</label>
                                <input type="text" name="phone" value="{{ isset($employee) ? $employee->phone : '' }}" placeholder="+92 300 1234567" class="w-full bg-gray-50 border border-gray-200 p-3 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none">
                            </div>
                            <div>
                                <label class="block text-xs font-bold text-gray-500 uppercase mb-2">Email Address*</label>
                                <input type="email" name="email" value="{{ isset($employee) ? $employee->email : '' }}" required placeholder="ali@example.com" class="w-full bg-gray-50 border border-gray-200 p-3 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none">
                            </div>
                        </div>

                        <div class="grid grid-cols-2 gap-6">
                            <div>
                                <label class="block text-xs font-bold text-gray-500 uppercase mb-2">Residential Address</label>
                                <textarea name="address" rows="2" placeholder="Street Address..." class="w-full bg-gray-50 border border-gray-200 p-3 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none">{{ isset($employee) ? $employee->address : '' }}</textarea>
                            </div>
                            <div>
                                <label class="block text-xs font-bold text-gray-500 uppercase mb-2">City</label>
                                <input type="text" name="city" value="{{ isset($employee) ? $employee->city : '' }}" placeholder="e.g. Karachi" class="w-full bg-gray-50 border border-gray-200 p-3 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none">
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Role & Compensation Card -->
                <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
                    <div class="flex items-center gap-2 mb-6 text-green-600">
                        <i class="fas fa-briefcase text-lg"></i>
                        <h3 class="font-bold text-gray-900 text-lg">Role & Compensation</h3>
                    </div>

                    <div class="grid grid-cols-2 gap-6">
                        <div>
                            <label class="block text-xs font-bold text-gray-500 uppercase mb-2">Designation / Role*</label>
                            <select name="designation" required class="w-full bg-gray-50 border border-gray-200 p-3 rounded-lg outline-none">
                                <option value="General Salesman" {{ isset($employee) && $employee->designation == 'General Salesman' ? 'selected' : '' }}>General Salesman</option>
                                <option value="Cashier" {{ isset($employee) && $employee->designation == 'Cashier' ? 'selected' : '' }}>Cashier</option>
                                <option value="Manager" {{ isset($employee) && $employee->designation == 'Manager' ? 'selected' : '' }}>Manager</option>
                                <option value="Accountant" {{ isset($employee) && $employee->designation == 'Accountant' ? 'selected' : '' }}>Accountant</option>
                                <option value="Stock Keeper" {{ isset($employee) && $employee->designation == 'Stock Keeper' ? 'selected' : '' }}>Stock Keeper</option>
                            </select>
                        </div>

                        <div>
                            <label class="block text-xs font-bold text-gray-500 uppercase mb-2">Employee ID (Legacy AC No)</label>
                            <input type="text" name="employee_code" value="{{ isset($employee) ? $employee->employee_code : '' }}" placeholder="EMP-001" class="w-full bg-gray-50 border border-gray-200 p-3 rounded-lg outline-none">
                        </div>

                        <div>
                            <label class="block text-xs font-bold text-gray-500 uppercase mb-2">Commission Rate (%)</label>
                            <div class="relative">
                                <input type="number" step="0.01" name="commission_rate" value="{{ isset($employee) ? $employee->commission_rate : '0.00' }}" class="w-full bg-gray-50 border border-gray-200 p-3 rounded-lg outline-none pr-8">
                                <span class="absolute right-3 top-3 text-gray-400">%</span>
                            </div>
                            <p class="text-[10px] text-gray-400 mt-1">Used for calculating sales commissions.</p>
                        </div>

                        <div class="flex items-center pt-6">
                            <label class="flex items-center space-x-3 cursor-pointer">
                                <input type="checkbox" name="is_active" {{ isset($employee) ? ($employee->is_active ? 'checked' : '') : 'checked' }} class="w-5 h-5 text-indigo-600 rounded focus:ring-indigo-500 border-gray-300">
                                <span class="text-sm font-medium text-gray-700">Active Employee Status</span>
                            </label>
                        </div>
                    </div>
                </div>

            </form>
        </div>

    </div>

    <script>
        // Search functionality
        const searchInput = document.getElementById('searchInput');
        const employeeCards = document.querySelectorAll('.employee-card');

        searchInput.addEventListener('input', (e) => {
            const searchTerm = e.target.value.toLowerCase();

            employeeCards.forEach(card => {
                const name = card.dataset.name;
                const email = card.dataset.email;
                const phone = card.dataset.phone;

                if (name.includes(searchTerm) || email.includes(searchTerm) || phone.includes(searchTerm)) {
                    card.style.display = 'flex';
                } else {
                    card.style.display = 'none';
                }
            });
        });
    </script>

</body>

</html>